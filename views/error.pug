doctype html
html(lang="en")
  include common/header.pug
  +head("starter MVP")
  body
    header
      include common/navbar.pug
      +navbar()

block content
  h1= message
  h2= error.status
  pre #{error.stack}
